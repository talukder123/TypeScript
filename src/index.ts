
type Student = {
    name : string,
    age : number,
    isSingle?: boolean
}

const firstStudent : Student = {
    name : "Talukder",
    age : 23,
    isSingle : true
}

console.log(firstStudent);
